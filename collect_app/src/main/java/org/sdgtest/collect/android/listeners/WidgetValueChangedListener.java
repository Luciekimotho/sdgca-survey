package org.sdgtest.collect.android.listeners;

import org.sdgtest.collect.android.widgets.QuestionWidget;

public interface WidgetValueChangedListener {
    void widgetValueChanged(QuestionWidget changedWidget);
}
